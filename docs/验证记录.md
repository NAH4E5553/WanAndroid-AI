# 阶段 1 验证记录

日期：2026-09-09。所有构建/格式化/写操作仅发生在 WanAndroid-AI；来源商城只读。

## 公开接口探测

参考：[玩 Android API 文档](https://wanandroid.com/blog/show/2)。只读取公开接口，无登录、收藏或其他服务端写操作。

- tree/json：errorCode=0，返回 61 个一级节点。
- cid=150（开发环境）：total=169；cid=60（其子类）：total=40。
- cid=167（基础知识）：total=111；cid=168（其子类）：total=21。
- 上述均请求 article/list/0/json?page_size=2，响应 curPage=1、over=false。
- 结论：抽样父类 ID 可直接请求，不需要先引入跨子类分页聚合；不是保证全部分类有内容。
- wenda/list/1/json：errorCode=0，20 条；应用取前 5 条。是否需要剔除置顶等特殊排序，在功能接入时确认响应语义，不自行按 ID 排序。
- 第一版列表标题采用“最新博文”，不声称公开首页列表按热度排序。

## HTML 可用性探测（非离线验收）

可复现命令：`python3 tools/probe_reading.py`。脚本只输出计数，不保存/打印文章全文。

| 样本 | HTTP | 字节数 | 去除 script/style 后字符数（含导航） |
|---|---|---|---|
| wanandroid.com/blog/show/3932 | 200 | 29269 | 504 |
| www.wanandroid.com/wenda/show/26578 | 200 | 55884 | 3612 |
| juejin.cn/post/7682769917025189903 | 200 | 112211 | 4567 |

这些结果只证明页面 HTML 可获取，不能证明提取到完整正文。短内容、跳转页、动态页面需进一步判断。
离线缓存阶段仍需：正文识别与净化、容量限制、Room 事务、断网读取、失败降级和可恢复任务测试。
不绕过登录、付费、反爬或证书限制，不承诺所有第三方站点可缓存。

## 构建与测试

首次离线 Gradle 配置因 error_prone_annotations 依赖未缓存而失败，联网获取后继续。
首次自定义插件编译修正了 apply 返回类型；架构检查排除了 Hilt 内部聚合产生的合成自依赖，但继续检查显式声明作用域中的所有模块边。

已确认：

- `:app:assembleDebug` 生成 Debug APK。
- core:network：3 个测试，0 失败，覆盖关键错误码缺失、登录失效、可空作者。
- core:data：7 个测试，0 失败，覆盖分页游标、末页、分类映射、问答条数、业务错误/空 data、IO 失败、取消传播。
- core:navigation：3 个测试，0 失败，覆盖连续返回、过期回调和根页面保留；页面快速点击已防止重复入栈。
- Room KSP 导出初始 Schema，history/offline 分表，无级联删除。

完整 Debug 验证已通过：`spotlessApply verifyArchitecture :app:assembleDebug testDebugUnitTest lintDebug`。
Lint 共 13 份模块报告，0 Error/Fatal，23 条依赖版本更新 Warning；按迁移约定暂不升级，不屏蔽这些提醒。
APK 元数据确认 applicationId=com.personal.wanandroid、App 名称 WanAndroid-AI、minSdk=23、targetSdk=36。

架构反例测试：`./gradlew verifyArchitecture -I tools/forbidden-edge.init.gradle.kts` 按预期拒绝 `:feature:home -> :feature:auth`。
反例仅修改此次调用的内存依赖图，不写入实际模块构建文件。后续正常验证不使用该 init script。
SDK 路径和构建产物已被 Git 忽略，独立仓库没有远端，也没有创建提交。

最终验证命令成功（退出码 0）：

```bash
./gradlew verifyArchitecture spotlessCheck :app:assembleDebug testDebugUnitTest lintDebug :app:assembleRelease --console=plain --quiet
```

最终共 13 个单元测试通过；Debug APK 约 13 MiB，未签名 Release APK 约 1 MiB（骨架业务尚未接入，不能据此估计完整版本体积）。
产物分别位于 app/build/outputs/apk/debug/app-debug.apk 与 app/build/outputs/apk/release/app-release-unsigned.apk。
Release 已完成代码和资源压缩，未使用正式签名。源码和所有未跟踪文本文件经过空白差异检查。
原商城以禁用可选 Git 锁和 fsmonitor 的只读 status 回查，仍无改动。

未验证：真机安装/运行、截图、大字体、进程恢复、Room 设备测试、Migration、Release 运行、Cookie、收藏、WebView 安全及正文离线链路。当前没有对应业务的完成声明。

## 防劣化边界

verifyArchitecture 当前检查已声明 Gradle 模块边、UI 对 network/database 的直接导入、Data 层导入 Compose、原商城包名残留及 GlobalScope。
这不是完整静态分析，不能替代对 UI 状态、协程竞争、安全和生命周期的代码审查及测试。
CI 文件已落地但未在 GitHub 执行，也未设置远端 Required Checks。

后续记录（2026-09-10，历史状态）：origin 已关联新仓库，CI/OCR 已完成本地适配；见 [CI-OCR本地适配记录](CI-OCR本地适配记录.md)。当时仍未提交或推送，OCR 未启用。

同日后续：用户反馈已自行配置模型凭据；本地完成 OCR 日志安全适配，37 项配置回归及完整 Android CI 等效检查通过。未读取凭据、修改远端开关或调用模型；配置先进入 main，再通过真实 PR 验证。详见上述记录的“OCR 日志安全修复”章节。

## 阶段 2 首页文章列表（2026-09-10）

GitHub 首次 Android CI 在提交 `247599c` 上实际产生六项成功检查：CI Configuration、Build、Unit Tests、Android Lint、Code Format、Architecture。
main 随后配置为受保护分支：要求 PR、分支同步、讨论解决和上述六项检查；审批人数为 0；管理员不可绕过；禁止强推和删除。当时 OCR 未启用，也未设为必需检查。

本地分支 `codex/feature-home-articles` 已完成首页文章列表第一批实现；以下为提交前的实现记录：

- `HomeRoute` 获取 Hilt ViewModel，并使用 `collectAsStateWithLifecycle` 收集状态；`HomeScreen` 只通过参数和事件渲染。
- 首页接入真实文章列表、下拉刷新、自动加载更多、空态、首次失败、刷新失败、追加失败重试和末页提示。
- 刷新取消旧任务并使用请求代次拒绝过期结果；追加请求去重，游标只在成功后推进，跨页文章按 ID 去重。
- 文章点击只组装现有 `ArticleRoute`；WebView 阅读仍是占位功能，不计作完成。
- Repository 增加可替换契约与 Hilt 绑定，Feature 单元测试无需接触网络实现。

新增 HomeViewModel 单元测试 10 个，覆盖成功、空、首次失败重试、失败后禁止误加载更多、刷新失败保留旧数据、追加去重、追加失败同页重试、重复追加、末页和过期请求竞争。连同原有测试共 23 个，0 失败。

完整本地验证命令成功（退出码 0）：

```bash
./gradlew verifyArchitecture spotlessCheck :app:assembleDebug testDebugUnitTest lintDebug --no-daemon --stacktrace
```

Lint 为 0 Error/Fatal；当前显示 27 条依赖或构建工具版本更新 Warning，本次不升级依赖。ADB 设备列表为空，未验证真机显示、下拉手势、实际分页滚动、深色、大字体、旋转和进程恢复，因此相关验收项保持未勾选。

后续用户反馈已在真机完成首页基本验证。本记录未取得设备型号、系统版本和逐项结果，因此不把该反馈扩展为深色、大字体、旋转、进程恢复或主题选择专项已通过。

## 多配色主题选择方案（2026-09-10）

用户确认默认值为“石板蓝 + 跟随系统”。已新增 [主题选择开发方案](主题选择开发方案.md)，并同步更新架构方案和第一版验收清单。

当前仅完成方案记录，尚未修改主题代码、引入 DataStore、增加主题选择界面或执行相关测试。已确认的设计边界：

- 墨青绿、石板蓝、暖琥珀、莓果玫瑰为四种配色；跟随系统、浅色、深色为独立显示模式，不把深色存成第五个互斥主题。
- 配色偏好属于本机，不归账号，退出不清除。
- “每日一问”使用中性表面和克制的主色侧边标记，不再使用大面积异色背景。
- 原生 Compose 主题和第三方 WebView 暗色能力分别验收，不承诺所有外部网页一致变暗。

该主题方案记录本身未修改主题代码或单独执行构建；主题功能及设备表现均保持未验证状态。随后它与首页功能一同进入 PR #1。

## 多配色主题选择实现（2026-09-10）

本地分支 `codex/feature-theme-selection` 已完成第一批实现，尚未提交、推送或创建 PR：

- `core:designsystem` 提供墨青绿、石板蓝、暖琥珀、莓果玫瑰四套完整浅深 `ColorScheme`，阅读表面统一使用中性色，移除旧主色 `#465CFF`。
- `core:data` 使用 Preferences DataStore 保存稳定字符串值，默认“石板蓝 + 跟随系统”；未知值和 `IOException` 安全回退，取消异常重抛，写入失败返回显式结果。
- app 根节点收集主题偏好，解析跟随系统/固定浅色/固定深色，并同步 Edge-to-edge 状态栏和导航栏图标明暗。
- “我的 → 外观与主题”提供 2 × 2 配色卡片和三段显示模式；选择即时反映，写入失败恢复最后一次持久化值并提示。选项同时提供文字、选中标记、单选语义和至少 48dp 的触摸区域。
- 快速连续选择会取消旧写入，并以请求代次拒绝过期结果更新界面。

新增 12 个单元测试：designsystem 2 个、core:data 5 个、feature:profile 3 个、app 2 个；连同原有测试共 35 个，0 失败。覆盖八套主色映射、中性表面和旧紫色防泄漏、稳定持久化值、默认/未知/读取失败/取消、写入成功/失败回滚/快速选择竞争，以及显示模式解析。

完整本地验证命令成功（退出码 0）：

```bash
./gradlew verifyArchitecture spotlessCheck :app:assembleDebug testDebugUnitTest lintDebug --no-daemon --stacktrace
```

验证通过架构、格式、Debug APK、单元测试和 Lint。尚未执行本次 Release 构建；尚未在真机验证四套配色、系统浅深切换、系统栏图标、大字体、屏幕阅读器、旋转、进程重启恢复及快速连点的实际观感，因此对应设备验收项保持未勾选。

## 首次真实 PR 与 OCR 验证（2026-09-10）

- 仓库已确认两个 OCR Secret 和三个 Variable 名称齐全；未读取或输出 Secret 值。
- 用户确认代码上下文可发送到已配置的模型端点并接受调用费用后，OCR_ENABLED 设为 true。
- PR #1 首次运行的 CI Configuration、Build、Unit Tests、Android Lint、Code Format、Architecture 全部成功。
- OpenCodeReview 首次真实模型调用成功，发布汇总“无发现”，没有行级评论；该结论不替代 CI、人工判断或设备回归。
- 首次文档同步的增量范围解析正确、模型命令退出码为 0，但返回结果未满足评论发布契约，工作流以 `OCR_COMMENT_POST_FAILED` 失败；日志保持原始结果抑制，清理步骤成功。随后排除 `.github/**/*.md` 单独触发，同时保留 `.github` 非 Markdown 配置的保守审查。
- Actions 产生 Node.js 20 兼容模式及 actions/setup-java@v4 弃用提醒，未影响本次检查；后续单独评估 Action 升级，不随本功能 PR 批量升级工具链。

## 首页文章元信息格式（2026-09-10）

按用户确认的展示规则调整首页文章卡片：

- `author` 非空时显示“作者：author值”；为空或全空白时显示“分享者：shareUser值”。
- 分类固定显示“分类：superChapterName值/chapterName值”。
- 时间固定显示“时间：niceDate值”。
- DTO 和业务模型分别保留作者、分享者、一级分类、二级分类和时间，不再在 Repository 中提前把分享者合并进作者字段。

core:data 新增 1 个映射测试，确认作者为空时仍保留分享者及两级分类字段；当前项目单元测试总数为 36。最新样式尚待真机确认。
